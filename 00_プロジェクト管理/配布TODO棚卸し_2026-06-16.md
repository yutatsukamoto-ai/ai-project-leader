# 配布TODO棚卸し（2026-06-16）

目的: `_tools/build.sh --release-check` が検出した `配布TODO` 18件を、配布前に本当に潰すべきものと、古いメモ・意図的プレースホルダーに仕分ける。

結論:
- **対応済み**: PMBOK基準資料の同梱系は `_tools/sync-manifest.tsv` に追加し、各子Skillの `references/` へ同期した。管理計画6Skillの古いTODO行も削除済み。
- **残**: WBS Viewer/更新フォームの同梱・帰属系と、`_seikabutsu-template` の方針待ち。
- 現在の `--release-check` 検出は **2件**（wbs-builder / _seikabutsu-template）。

## A. 本当に対処が要る配布ブロッカー

| 種別 | 対象 | 必要な対処 |
|---|---|---|
| PMBOK 40プロセス同梱 | `stakeholder-register` / `poc-teiansho` / `scope-plan` / `communication-plan` / `schedule-plan` / `cost-plan` / `resource-plan` / `risk-plan` / `wbs-builder` / `communication-management` / `resource-management` / `issue-log` / `status-report` / `change-log` / `project-closeout` / `poc-evaluation` | **対応済み**: `10_参考資料/PMBOK第8版_40プロセス一覧.md` を各 `references/` へ同梱し、対象子Skillの参照先をSkill内パスへ寄せた |
| PMBOK 40プロセス＋6原則同梱 | `project-charter` / `integrated-plan` | **対応済み**: `PMBOK第8版_40プロセス一覧.md` と `PMBOK第8版_6原則一覧.md` を各 `references/` へ同梱し、参照先をSkill内パスへ寄せた |
| WBS外部資産 | `wbs-builder` | **未対応**: `WBS_更新依頼テンプレ.md`、WBS Viewer一式を配布物に入れるかを決める。WBS Viewer同梱時はMITライセンスと帰属を明記する |

## B. 古いTODOの可能性が高いもの

| 種別 | 対象 | 判断 |
|---|---|---|
| 文章作法・表と数値の見せ方 | `scope-plan` / `communication-plan` / `schedule-plan` / `cost-plan` / `resource-plan` / `risk-plan` | **対応済み**: 各 `references/` に実体があり、`build.sh --check` で同期対象にも入っているためTODO行を削除した |

## C. 意図的プレースホルダー

| 種別 | 対象 | 判断 |
|---|---|---|
| テンプレ用TODO | `99_メタ/_seikabutsu-template` | 新規Skill作成時に埋めるための雛形文。配布対象に含めるなら注意として残す。release-checkの対象から除外するなら、検査関数側に例外を足す |

## 次アクション

1. `wbs-builder` の更新フォームとWBS Viewer一式を、配布物へ同梱するか外部取得にするか決める。
2. `_seikabutsu-template` を配布対象にするかを決める。配布対象外なら `--release-check` の例外にする。
3. 上記2件が解けたら、`bash _tools/build.sh --release-check` を配布前ゲートとして緑にする。
