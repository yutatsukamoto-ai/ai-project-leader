# 配布TODO棚卸し（2026-06-16）

目的: `_tools/build.sh --release-check` が検出する `配布TODO` 18件を、配布前に本当に潰すべきものと、古いメモ・意図的プレースホルダーに仕分ける。

結論:
- 配布ブロッカーとして扱うべきものは **PMBOK基準資料の同梱系** と **WBS Viewer/更新フォームの同梱・帰属系**。
- 管理計画6Skillの「文章作法・表と数値の見せ方を同梱」は、現時点では各 `references/` に実体があるため、古いTODOの可能性が高い。配布前に実体確認してTODO行を削除する候補。
- `_seikabutsu-template` のTODOはテンプレ内プレースホルダー。配布対象に含めるか、release-checkから除外するかを配布方針で決める。

## A. 本当に対処が要る配布ブロッカー

| 種別 | 対象 | 必要な対処 |
|---|---|---|
| PMBOK 40プロセス同梱 | `stakeholder-register` / `communication-management` / `resource-management` / `issue-log` / `status-report` / `change-log` / `project-closeout` / `poc-evaluation` | `10_参考資料/PMBOK第8版_40プロセス一覧.md` を各 `references/` へ同梱し、SKILL.mdの参照先をSkill内パスへ寄せる |
| PMBOK 40プロセス＋6原則同梱 | `project-charter` / `integrated-plan` | `PMBOK第8版_40プロセス一覧.md` と `PMBOK第8版_6原則一覧.md` を各 `references/` へ同梱し、参照先をSkill内パスへ寄せる |
| WBS外部資産 | `wbs-builder` | PMBOK一覧、`WBS_更新依頼テンプレ.md`、WBS Viewer一式を配布物に入れるかを決める。WBS Viewer同梱時はMITライセンスと帰属を明記する |

## B. 古いTODOの可能性が高いもの

| 種別 | 対象 | 判断 |
|---|---|---|
| 文章作法・表と数値の見せ方 | `scope-plan` / `communication-plan` / `schedule-plan` / `cost-plan` / `resource-plan` / `risk-plan` | 現在は各 `references/` に `文章作法.md` と `表と数値の見せ方.md` が存在する。配布前に `build.sh --check` で同期確認し、問題なければTODO行を削除する候補 |

## C. 意図的プレースホルダー

| 種別 | 対象 | 判断 |
|---|---|---|
| テンプレ用TODO | `99_メタ/_seikabutsu-template` | 新規Skill作成時に埋めるための雛形文。配布対象に含めるなら注意として残す。release-checkの対象から除外するなら、検査関数側に例外を足す |

## 次アクション

1. Aを解消する場合は、正典をコピーして `_tools/sync-manifest.tsv` に同期対象を追加するか、配布専用の同梱処理を作る。
2. Bは実体確認済みならTODO行を削除して、`--release-check` のノイズを減らす。
3. Cは `_seikabutsu-template` を配布対象にするかを決めてから扱う。

