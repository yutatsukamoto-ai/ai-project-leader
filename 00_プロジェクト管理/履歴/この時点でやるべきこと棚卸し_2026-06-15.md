# この時点でやるべきこと 棚卸し（2026-06-15）

> ⚠️ **後継あり**: 同日夜の決着（grill-me新設・D-02/D-04決着・D-03保留・基盤修正）を反映した最新版は `この時点でやるべきこと棚卸し_2026-06-15_v2.md`。本ファイルは朝の構造整理セッションの記録（履歴）。最新の現在地・次の一手はv2を見る。

前版: `この時点でやるべきこと棚卸し_2026-06-14.md`。本版は同日(06-15)の構造整理セッションを反映。
出典: 懸念マスター(最新)・接続規約v0.2・決定の索引・バックアップとバージョン管理・骨子(最終ゴール=配布)。

---

## 今セッション(06-15)で完了

- 成果物マップの二重化解消（`20_Skills/成果物マップ.md`をSSOT確定・旧v3アーカイブ）。
- M-10 chain配布方針を明記 ／ M-11 案件フォルダを規約整合（クライアントAを決定4移行・クライアントBを適用外・contextの作成タイミング明記）／ M-12 横断の住み分け＝✅。
- 再発防止「規約・枠を新設するときの心得」（遡及／空枠の理由／適用条件）。
- map-check（成果物マップ↔実構成の自動照合）を `build --verify` に実装＋故障注入で検知実証。
- 「決定の索引」新設（蒸し返し防止）。
- バックアップ（`_backups/` tgzスナップショット）＋ git導入（初回コミット完了）。
- （前段で）stakeholder-register #23 ＝立ち上げフェーズが完全体に。

## 残り（カテゴリ別）

### A. 決定待ち（壁打ち・低工数・高アンロック）

- **D-01 実データ匿名化・Flow→Stock規約** ← キーストーン。M-09／M-11残／クライアントAのcontext／配布、すべての前提。
- D-02 運用イメージ（都度コマンド型か、常時伴走型か）。
- D-03 マイルストーン・期限の設定。
- D-04 二周目スコープ（成果物マップのどこを線で囲むか）。

### B. 配布パッケージ化（最終ゴール）

- 配布物の最終形・README入口の仕上げ（3層モデルの配布物層を「配る一式」に詰める）。
- M-08 ポータビリティ検証（Copilot等で何が動かないかを1点、実地確認）。

### C. 使い込み・実証で広げる

- 3案件目を入力形式を変えて実証（議事録／スライド入力）＝実証の幅＋evalゴールデンの厚み（M-09も前進）。
- 計画・実行フェーズの厚み（スケジュール／コスト／品質・デリバラブル生成）＝テーラリングで「要る案件だけ」作る。

### D. Claude Code版（設計凍結済・coverage進展後）

- M-05 停止ポイントのHooks強制 ／ eval自動化（LLM-judge・Hook/CI）／ M-08確定。
- 設計は `Claude_Code版を作るときの覚書_モデル堅牢性.md` §3に固定済。

### E. 発動条件つきの宿題（今やらない）

- #2 正典レジストリ（正典が4つ目に増えたら）／ #4 空フォルダ規約（空フォルダが落ちて困ったら）／ 横断⑤出し分け・⑥命名（複数案件を回してから）。

---

## 推奨実行順（無駄なく）

1. **D-01 を壁打ちで決める** … キーストーン。決定なので工数は小さいのに、Flow→Stock・学習還流・クライアントAの継続・配布の前提を一気に外す。**どのゴールへ進むにも必要＝先にやって損がない**。
2. **分岐（ゴール次第）**
   - 配布を急ぐなら → 配布物の仕上げ → M-08。
   - 使い込みで固めるなら → 3案件目（入力形式を変える）→ 計画/実行の厚み。
3. **coverageが進んだら** → Claude Code版（Hooks強制・eval自動化）。
4. **宿題(E)** は発動条件が来たらClaudeから提案。

> 原則: 「安い決定で多くを外す（D-01）」を先に。重い実装（配布物・Claude Code版）は方向が定まってから。すべて可逆・テーラリング前提で、要らないものは作らない。
